source("renv/activate.R")

options(
  encoding = "UTF-8",
  vsc.rstudioapi = TRUE,
  repos = c(
    nyuglobalties = "https://nyuglobalties.r-universe.dev",
    CRAN = "https://cran.rstudio.com/"
  )
)

if (interactive() && identical(getOption("browser"), "")) {
  options(browser = "firefox")
}
